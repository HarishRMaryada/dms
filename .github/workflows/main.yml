name : dms
on : [push]
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: checkout Repo
        uses: actions/checkout@v3
      - name : setup Nodejs
        uses: actions/setup-node@v4.0.2
       
